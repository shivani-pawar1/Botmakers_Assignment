# Botmakers_Assignment
This project is a full-stack Role-Based Access Control (RBAC) system developed using Spring Boot (backend) and React + TypeScript (frontend). It provides secure authentication and authorization using JWT (JSON Web Token).  The application allows users to register, login, and access resources based on their assigned roles (e.g., ADMIN, USER).
